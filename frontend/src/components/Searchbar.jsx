const Searchbar = () => {
    return (
        <input
            className="form-control me-2"
            type="search"
            placeholder="Buscar"
            aria-label="Search"
        />
    );
}

export default Searchbar;